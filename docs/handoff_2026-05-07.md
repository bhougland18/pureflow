# Pureflow Handoff - 2026-05-07

## Current State

No bead is currently in progress.

The previous implementation wave closed the WASM productization and product
documentation epics. The Beads queue was empty except for the intentionally
deferred data-tier parking lot, so the backlog was regrouped around release
automation, authoring ergonomics, runtime hardening, and workload discovery.

## New Epics

The next open epics are:

- `cdt-2av`: Epic 14: Release Candidate Automation and Packaging
- `cdt-dtx`: Epic 15: CLI and Workflow Authoring UX
- `cdt-4be`: Epic 16: Node Authoring SDK and Templates
- `cdt-jrf`: Epic 17: Workload Discovery Before Data Tier
- `cdt-a1s`: Epic 18: Runtime Hardening and Regression Guardrails

`cdt-pyg` remains deferred. Do not undefer Arrow/DataFusion work until the
workload discovery epic produces concrete pressure.

## Ready Beads

Use `br ready --json` to refresh before starting. The initial ready list after
backlog creation is:

1. `cdt-2av.1`: ci-full-validation-gate
2. `cdt-2av.3`: workspace-package-metadata-audit
3. `cdt-dtx.1`: cli-migrate-to-clap
4. `cdt-dtx.3`: workflow-and-manifest-schema-generation
5. `cdt-4be.1`: native-node-helper-apis
6. `cdt-4be.2`: wasm-guest-template-and-wit-docs
7. `cdt-4be.5`: node-authoring-error-patterns
8. `cdt-a1s.1`: metadata-json-snapshot-tests
9. `cdt-a1s.2`: supervisor-failure-matrix-tests
10. `cdt-a1s.3`: public-api-substrate-boundary-audit
11. `cdt-a1s.4`: watchdog-policy-cli-and-docs
12. `cdt-a1s.5`: criterion-baseline-capture

Recommended next bead:

- Start `cdt-a1s.1` if you want a low-risk guardrail before changing CLI or
  runtime behavior.
- Start `cdt-dtx.1` if you want to improve the user-facing command surface
  immediately.
- Start `cdt-2av.1` if the next goal is release-candidate discipline.

## Notes

The new backlog intentionally avoids reopening the data-tier epic. The current
byte-message and WASM surfaces should be exercised by representative workloads
before adding Arrow or DataFusion complexity.

The most useful near-term sequencing is:

1. Add metadata/run-summary snapshots.
2. Move the CLI parser to `clap`.
3. Add native node helper APIs.
4. Use those helpers to build workload examples.
5. Review whether the workload examples justify undefering any data-tier work.

## Validation

After creating the backlog, dependency cycles were checked:

```sh
br dep cycles --json
```

Result: no cycles.
