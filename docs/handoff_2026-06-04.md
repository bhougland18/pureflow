# Pureflow Handoff - 2026-06-04 (Rules Engine Implementation)

## Current State

Implementing the rules engine epic (`pu-4dm`) bead by bead. Each bead is a
separate branch stacked on the previous one. **Nothing is pushed** — SSH auth
is unavailable in the agent session; the user pushes manually when asked.

### Branch chain (each stacked on the prior)

```
main
 └─ mayor/pu-256-rules-types-core         (pu-256 ✓ closed)
     └─ mayor/pu-n8y-rules-eval-metadata  (pu-n8y ✓ closed)
         └─ mayor/pu-f2q-rules-evaluator  (pu-f2q ✓ closed)
             └─ mayor/pu-yda-rules-native-node      (pu-yda ✓ closed)
                 └─ mayor/pu-u0b-rules-metadata-jsonl  (pu-u0b ✓ closed)
                     └─ mayor/pu-213-rules-introspection  (pu-213 ✓ closed)
                         └─ mayor/pu-yg6-rules-set-source     (pu-yg6 ✓ closed)
                             └─ mayor/pu-rna-rules-format-loading  (pu-rna CLAIMED, NOT STARTED)
```

**IMPORTANT**: The first commit on `mayor/pu-256...` also contains the deferred
initial pureflow codebase publish + watcher-cancellation example (pu-ea9). The
repo had the whole codebase staged-but-uncommitted; it went in with pu-256.
This large commit still needs human review before any merge to main.

## Completed Beads (all closed with detailed close reasons in `bd show <id>`)

| Bead | What landed |
|---|---|
| pu-256 | `pureflow-rules` crate: Condition (20 variants), RuleAction, Rule, RuleSet, EvaluationStrategy, RuleDecision, EvalContext, RuleError. AllMatches=Tag-only enforced at construction. serde feature. Added serde+TryFrom<String> to pureflow-types. |
| pu-n8y | `pureflow-core::metadata`: RuleEvalRecord, ConditionTrace, RuleEvalStrategy, RuleEvalAction, ConditionSurfaceRecord. MetadataRecord::RuleEval variant + JSON serialization. Defined in pureflow-core to avoid circular dep with pureflow-rules. |
| pu-f2q | `RuleSetEvaluator::evaluate` pure function. All 20 condition variants, 3 strategies (FirstMatch/AllMatches/Score), tag accumulation, ConditionTrace when trace_conditions=true. Removed `payload` field from EvalContext (passed separately). |
| pu-yda | `RuleNode` NodeExecutor. recv→build EvalContext→evaluate→execute action. CONTRACT_ID="pureflow.rules.v1". Drain guarantee: mid-packet always completes before cancellation check. |
| pu-u0b | RuleNode emits RuleEvalRecord via `.with_metadata_sink()`. Emitted BEFORE the send so a cancelled send still records. action_to_eval/strategy_to_eval/scalar_to_json helpers. |
| pu-213 | `pureflow-introspection::rules`: RuleSetIntrospection via introspect_rule_set(). Condition surface breakdown, target ports, unreachable detection (Never/Always-shadow/exact-FieldEq-subsumption). |
| pu-yg6 | `pureflow-workflow-format::rule_source`: RuleSetSource trait (BoxFuture), SourceRegistry (scheme dispatch + LocalFsSource fallback), LocalFsSource, EmbeddedSource, RuleSourceError. No global singleton. |

## NEXT: pu-rna (rules-format-loading) — claimed, not started

Branch `mayor/pu-rna-rules-format-loading` is checked out. Goal: wire inline +
side-car rule set loading into the raw workflow document parsing in
`pureflow-workflow-format`. The `RawNodeDefinition` needs optional `rule_set`
(inline JSON) and `rule_set_ref` (URI) fields; resolve via SourceRegistry at
load time and inline the resolved RuleSet before handing to validation.

See `crates/pureflow-workflow-format/src/lib.rs` (RawWorkflowDefinition,
RawNodeDefinition) and `src/rule_source.rs` (SourceRegistry::load).

## Remaining open beads after pu-rna

- `pu-dnl` rules-contract-validation (deps pu-256 ✓) — Route/DeadLetter/SourceNode/ArrivedOnPort validation in pureflow-contract
- `pu-qql` rules-cli-explain (deps pu-213 ✓) — CLI plain-text rule routing. NOTE: pureflow-cli has a pre-existing compile bug (pu-5dh, CONDUIT_TRACE_ENV undefined in tests) that blocks `cargo test -p pureflow-cli`.
- `pu-bm4` rules-wasm-wit (P3, deps pu-256 ✓)
- `pu-b4j` rules-wasm-host (P3, deps pu-bm4 + pu-yda)
- `pu-2b9` rules-example-routing (deps pu-yda, pu-u0b, pu-qql, pu-dnl, pu-rna)

## Build / Test Commands

Cargo is NOT on PATH directly. Use the nix devshell:

```sh
cd /home/ben/code/gt/pureflow/refinery/rig
nix develop --command cargo test -p pureflow-rules
nix develop --command cargo check --workspace
```

asupersync is pinned at 0.2.9 (workspace dep). Working dir for all rig git ops
is `/home/ben/code/gt/pureflow/refinery/rig` (the mayor/rig clone is empty;
work happens in refinery/rig on mayor/* branches).

## Workflow per bead

1. `cd /home/ben/code/gt/pureflow/mayor/rig && bd update <id> --claim`
2. `git -C <refinery/rig> checkout -b mayor/<id>-<slug> mayor/<prev-branch>`
3. Implement + test via nix devshell
4. `git -C <refinery/rig> add <files> && git commit` (Co-Authored-By: Claude)
5. `bd close <id> --reason "..."`

## Files

- Proposal: `docs/archetecture/rules-engine-proposal.md`
- Design handoff: `docs/archetecture/rules-engine-handoff.md`
- This session handoff: `docs/handoff_2026-06-04.md`
